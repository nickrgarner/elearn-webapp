<%= form_with(model: [@student, @credit_card], local: true) do |form| %>
  <% if credit_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(credit_card.errors.count, "error") %> prohibited this credit_card from being saved:</h2>

      <ul>
      <% credit_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :credit_card_name %>
  </div>

  <div class="field">
    <%= form.label :card_number %>
    <%= form.number_field :card_number, id: :credit_card_card_number %>
  </div>

  <div class="field">
    <%= form.label :expiration_date %>
    <%= form.month_field :expiration_date, id: :credit_card_expiration_date %>
  </div>

  <div class="field">
    <%= form.label :cvv %>
    <%= form.number_field :cvv, id: :credit_card_cvv %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
